# ====================================================
# WELLNEST CONFIGURATION FILE
# ====================================================
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to version control
# ====================================================

# ============== REQUIRED CREDENTIALS ================

# NVIDIA NIM API Configuration (REQUIRED)
# Sign up at: https://build.nvidia.com/
# Get your API key from the dashboard
NIM_API_KEY=nvapi-YOUR_KEY_HERE  # <-- REPLACE WITH YOUR NVIDIA API KEY
NIM_MODEL_NAME=nvidia/nv-embedqa-e5-v5

# MongoDB Configuration (REQUIRED)
# Create free cluster at: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGO_URL=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/wellnest?retryWrites=true&w=majority  # <-- REPLACE
MONGO_DB_NAME=wellnest

# Alternative MongoDB configuration (if not using connection string)
MONGODB_USERNAME=YOUR_USERNAME  # <-- REPLACE
MONGODB_PASSWORD=YOUR_PASSWORD  # <-- REPLACE
MONGODB_CLUSTER=YOUR_CLUSTER.mongodb.net  # <-- REPLACE
MONGODB_DATABASE=wellnest

# ============== OPTIONAL CREDENTIALS ================

# Qdrant Vector Database Configuration (OPTIONAL)
# For cloud: Sign up at https://cloud.qdrant.io/
# For local: Use localhost:6333 and leave API key empty
QDRANT_URL=localhost:6333  # <-- Use localhost:6333 for local or https://YOUR_CLUSTER.qdrant.io for cloud
QDRANT_API_KEY=  # <-- Leave empty for local or add YOUR_QDRANT_KEY for cloud

# AWS Configuration (OPTIONAL - for deployment only)
# Only needed if deploying to AWS EKS
AWS_ACCOUNT_ID=123456789012  # <-- REPLACE if deploying to AWS
AWS_REGION=us-east-1  # <-- REPLACE with your preferred region
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE  # <-- REPLACE if deploying to AWS
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY  # <-- REPLACE if deploying to AWS

# ============== LOCAL DEVELOPMENT DEFAULTS ================
# These settings work out-of-the-box with docker-compose

# Kafka Configuration (auto-configured with Docker)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_EVENTS=sensor-events
KAFKA_BROKER_ID=1
KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=INSIDE:PLAINTEXT,OUTSIDE:PLAINTEXT
KAFKA_ADVERTISED_LISTENERS=INSIDE://kafka:29092,OUTSIDE://localhost:9092
KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1
KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1
KAFKA_AUTO_CREATE_TOPICS_ENABLE=true

# Redis Configuration (auto-configured with Docker)
REDIS_URL=redis://localhost:6379

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Dashboard Configuration
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Logging Configuration
LOG_LEVEL=INFO
USE_JSON_LOGGING=false

# ============== READ-ONLY DEMO CREDENTIALS ================
# For hackathon judges/testers - These are read-only credentials
# Uncomment the section below if you want to provide demo access

# DEMO_NIM_API_KEY=nvapi-DEMO_KEY_HERE  # <-- Add read-only NVIDIA key
# DEMO_MONGO_URL=mongodb+srv://demo_user:demo_pass@demo-cluster.mongodb.net/wellnest_demo?retryWrites=true&w=majority
# DEMO_QDRANT_URL=https://demo-cluster.qdrant.io
# DEMO_QDRANT_API_KEY=DEMO_QDRANT_KEY

# ====================================================
# NOTES:
# - Never commit .env file with real credentials
# - For production, use environment variables or secrets manager
# - MongoDB Atlas requires IP whitelisting (add 0.0.0.0/0 for testing)
# - NVIDIA API has rate limits on free tier
# ====================================================